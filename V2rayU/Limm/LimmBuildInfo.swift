// LimmBuildInfo.swift — overwritten by CI with real version + commit hash.
// Local dev builds show "dev / local" — that's expected.
enum LimmBuildInfo {
    static let version = "dev"
    static let commit  = "local"
}
